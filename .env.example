# Database (used by the app and docker-compose for MySQL root)
DB_HOST=db
DB_USER=vallarta
DB_PASS=vallarta
DB_NAME=vallarta

# MySQL root password (for initial DB creation; PHPMyAdmin can use root or DB_USER)
MYSQL_ROOT_PASSWORD=rootpassword
